# EctoBackup

Backup (and restore) for small to medium projects.

## Installation

The package can be installed by adding `ecto_backup` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:ecto_backup, "~> 0.1.0"}
  ]
end
```

